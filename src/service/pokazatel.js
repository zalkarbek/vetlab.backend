const Service = require('./service');

class PokazatelService extends Service {
  // ========================= REFERENCE ================================//
}

module.exports = new PokazatelService({ modelName: 'sPokazatel' });
